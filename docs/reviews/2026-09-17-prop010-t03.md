# Ronda T03 — contexto fresco sobre PROP-010 (registro de transcripción)

## Capa humana

Esta ronda la ejecutó **otra herramienta y otro modelo** (Codex, modelo
autodeclarado GPT-6) el 2026-09-16, en sandbox de sólo lectura, sobre la
propuesta cuando aún era un archivo sin versionar en la rama, con la base
`a918ceb`. Su veredicto fue **`fix-and-retry`**: dos hallazgos HIGH
—preautorización de mitigaciones confundible con autoridad de ejecución, y
minimización de datos sin frontera operativa—, tres MED y uno LOW.

Este archivo existe porque la ronda vivía fuera de todo repositorio y el gate
de reportes no la descubría (`skevi-gate.json` → `reports.dir =
docs/reviews`). Aquí se distinguen dos cosas que no deben confundirse:

- **La capa técnica de abajo es un envoltorio de registro**, emitido por quien
  transcribe, no por quien revisó. Su `report_sha256` acredita la forma de
  este registro, no la corrección de la ronda.
- **El informe original se transcribe textual**, sin editar, en su propio
  bloque. No cumple ADR-016 —no declara `report_sha256` ni bloque de
  trazabilidad— y no se pretende que lo cumpla: es evidencia externa citada,
  no un reporte de este repositorio.

La transcripción de sesión completa (5443 líneas: trazas de ejecución,
errores del lanzador y conteo de tokens) **no se versiona**: excede el límite de
tamaño del gate por un factor de siete y contiene ruido de herramienta, no
norma. Queda identificada por ruta y hash abajo, con el riesgo residual
declarado.

La disposición de los seis hallazgos vive en PROP-010 §6.2, no aquí: este
archivo registra lo que la ronda dijo; la propuesta registra qué se hizo con
ello.

## Capa técnica del registro (envoltorio, no es la ronda)

```text
id = SKV-RND-PROP010-T03-20260917-01
date = 2026-09-17
time_utc = 20:11:38Z
head_sha = 50e92fd04b23bf3c73ce4600781e2b6ada8f3d7a
model = Claude Opus 5 (self-declared) — transcriber, not reviewer
STATE = OK (transcription and record complete; scope is the record, not the round's subject matter)
GATE = Place the T03 fresh-context round under version control and inside the report gate's discovery path, distinguishing this wrapper from the external report it cites.
EVIDENCE
- Source transcript /Users/krisnova/www/aria/prop-010-t03.log, outside any repository -> SHA-256 fa2cdbb7354883c86df5cdf0bc852aae3a7ff284c51d20c6005716d79f78f689, 5443 lines -> pass
- Final report extracted from the last '## Technical layer' heading of that transcript to EOF -> 47 lines, transcribed verbatim below, no edits -> pass
- Reviewer identity -> Codex, model GPT-6 self-declared from environment; effective identity independently unverified -> inconclusive
- Reviewed artifact per the round itself -> PROP-010 SHA-256 c389ca325b32f7b3fcb6fd301c55414ec739e52c59a5f2f1e8d9ffc963b1dbe0 at base a918ceb -> pass
- Artifact as committed in 50e92fd04b23bf3c73ce4600781e2b6ada8f3d7a -> git show 50e92fd:docs/proposals/PROP-010-destilacion-engineering.md | shasum -a 256 reproduces c389ca32...963b1dbe0, identical to what the round reviewed -> pass
- Round verdict as transcribed (cited datum, not this record's decision) -> fix-and-retry; 2 HIGH, 3 MED, 1 LOW -> pass
- python3 scripts/check_reports.py with this record present -> 8 reports verified, 2 exempt, exit 0 -> pass
- Parent commit of this record -> 50e92fd04b23bf3c73ce4600781e2b6ada8f3d7a, the commit that placed PROP-010 under version control; this record and the proposal's corrections land in the next commit -> pass
- Upstream provenance of Engineering 1.2.0 (PROP-010 1.1) -> not verifiable inside the round's sandbox; remains a producer claim -> inconclusive
PENDING = Outside this record: a new fresh-context round over the corrected proposal (PROP-010 6.3) and human acceptance with folio (PROP-010 7). Within this record: nothing.
DECISION = proceed (scope: this transcription and record only; the cited round's own verdict is fix-and-retry and is dispositioned in PROP-010 6.2, not here)
RISK = The full session transcript is not under version control by size decision: if that file is lost, the hash above becomes unverifiable and only this transcription survives. This wrapper attests the form of the record, never that the cited round's findings are true, nor that its reviewer is who it claims to be.
report_sha256 = 5e3ab90d2fb4e0d78c411c3a0a9eeea13a35861532fd4f9e2a218dc0cf3f86ff
```

## Informe original de T03, textual

Transcrito sin editar. No es un reporte conforme a ADR-016 y no se valida como
tal. Lo que efectivamente lo deja fuera del gate es la ausencia de
`report_sha256`: `check_reports.py` ancla la capa en esa clave y su
`FENCE_RE` acepta cualquier etiqueta de fence, así que la etiqueta no
protege de nada por sí sola. `quoted-external-report` es señal **para
lectores humanos**, para que nadie tome por capa de este repositorio un
bloque que lleva su propio `STATE`, `model` y `DECISION`, que son los de la
ronda citada.

```quoted-external-report
## Technical layer (EN, authoritative)

STATE = PARTIAL  
GATE = adversarial review of PROP-010

EVIDENCE:

- `git status --short --branch; git rev-parse HEAD` -> branch `docs/prop-010-engineering-distillation`, HEAD `a918cebae885bea0c561db19ffc693d117df8b87`; proposal is untracked. Final observation matches initial state. No files modified, Git mutations, installations or network access performed. [pass]
- `shasum -a 256 docs/proposals/PROP-010-destilacion-engineering.md` -> reviewed artifact SHA-256: `c389ca325b32f7b3fcb6fd301c55414ec739e52c59a5f2f1e8d9ffc963b1dbe0`. [pass]
- All requested preliminary documents -> read completely; additionally inspected ADR-017, ADR-020, ADR-026, ADR-009, comparator/report implementation and historical PROP examples. [pass]
- `PROP-010 §1.1` -> upstream copy unavailable within scope; hashes, licensing, missing NOTICE, chronology and connector explanations remain producer claims. No independent verification possible here. [inconclusive]
- `PROP-010 §2` -> previous Operator decisions are attributed declarations; their original acceptance records were not supplied. Section 7 correctly leaves formal acceptance pending. [inconclusive]
- `python3 scripts/check_sizes.py` -> exit 0; 133 text files checked, reading path **993/1000**, gate/v6. `wc -l` independently confirms `105 + 114 + 501 + 273 = 993`; script occupies 867/870 lines. Launcher cache-write diagnostics occurred, but execution completed. [pass]
- `PROP-010 §3.3` -> **995** follows if implementation adds exactly two physical lines. Final line wrapping, pointer wording and conditional-reading semantics do not exist yet; final budget compliance remains unverified. Mandatory reading must not be hidden outside the measured path, per ADR-025. [inconclusive]
- `python3 scripts/check_reports.py; python3 scripts/check_plans.py` -> both exit 0: seven reports checked, two exempt; five plans checked. These results cover existing discovered artifacts, not future PROP-010 implementation. [pass]
- `scripts/check_templates.py`, in-memory simulation using its existing loaders and `main()` -> current-v7 control returns “al día”; simulated nonbreaking v7→v8 produces optional recopy notices for all three new templates, exit 0. No fixture files written. [pass]
- `templates/skevi/MANIFEST.json`, `docs/MANIFEST.json`, ADR-020/032 and existing validators -> added Markdown files, updated digests/history and corpus bump fit current schemas. No script change is inherently required. Drift requires a consumer installation record and an explicit comparison against the updated source; it is not automatic notification. [pass]
- `project-manifest.yaml:no_ofrece`, ADR-026, standard §§2.3–2.4 and `PROP-010 §3.4` -> no categorical prohibition against incident-process templates. Section 3.4 addresses the declared expansion, but acceptance must preserve the distinction between process guidance and runtime execution, assurance, domain governance or guarantees about published artifacts. [pass]
- Guide `02 §3.3`, historical PROP-006/009 and `PROP-010` header/§§7–8 -> compact deliberation is compatible with separate ADR creation and archival during implementation. Report validation has a separate gap described below. [pass]

FINDINGS:

- [HIGH] **The preauthorization checklist can be mistaken for sufficient execution authority.** — impact: an old or copied runbook can contain an exact action, trigger, scope, approver and approval date while its authorization has been revoked or its environment has changed. “What is not listed remains gated” leaves listed actions ambiguously exempt. The explicit Skevi exception is correct, but other adopters’ stricter policies also apply. — evidence: `PROP-010 §3.2 REQ-P10-04`; standard §§1.4–1.7, 4.3, 6.6–6.8; guide `04 §7`; ADR-017/018. — fix: make preauthorization a reference to recorded acceptance, including folio and the approved runbook revision; require verification of current scope, validity/revocation and applicable local restrictions before each execution. Cover rollback separately and stop on uncertainty. Preauthorization itself is not prohibited; treating checklist contents as authority is the defect.

- [HIGH] **Data minimization lacks an operational acceptance boundary.** — impact: “internal identifiers” can still identify a person or link to clinical information. A sanitized final report can also leave earlier sensitive drafts in Git history. The proposed gates cannot detect either case. — evidence: `PROP-010 §3.2 REQ-P10-03` permits internal identifiers but specifies no reference constraints or review before persistence; standard §§2.3, 2.4 and 7 prohibit sensitive material in reports/history; `project-manifest.yaml:no_ofrece` limits gates to structural verification. — fix: define permitted non-identifying incident references, exclude patient/account identifiers and identifying URLs, keep raw evidence outside Git, and require a documented sanitization check before staging/committing. Include synthetic acceptance cases covering identifiers, linked evidence and previously committed drafts. State explicitly that this is procedural enforcement, not automated detection.

- [MED] **The compact review record falls outside the report gate.** — impact: appending rounds to this proposal, then moving it to `docs/history/`, allows malformed technical layers or broken hashes to escape the normal verification command. — evidence: `PROP-010` header and §6.2; `skevi-gate.json:reports.dir = docs/reviews`; `scripts/check_reports.py:declared_reports()` scans that directory only. An in-memory discovery check returned `proposal_in_report_discovery=False`. The currently pending proposal has no canonical technical block; that alone is not a premature-review defect. — fix: preserve the requested compact file, but define an explicit read-only validation invocation for its embedded canonical layers and retain the result. Distinguish verbatim external reviews from any separately generated gate-compatible wrapper. No script modification is needed.

- [MED] **The maturity exit criterion accepts unsuccessful application.** — impact: a failed real-world pilot with recorded evidence satisfies “applied in at least one real case,” allowing removal of the experimental label without demonstrating usefulness or resolving serious findings. — evidence: `PROP-010 §3.5`; standard §1.3; guide `04 §5.3`. — fix: require predefined acceptance criteria, results tied to the tested template revision, disposition of findings and explicit Operator acceptance. One case must remain evidence of that case, not general validation.

- [MED] **The tech-debt rejection misapplies ADR-009.** — impact: it presents subjective prioritization as normatively forbidden, although ADR-009 governs task classification and rigor. This can incorrectly prevent an adopter from prioritizing debt while retaining mandatory task gates. — evidence: `PROP-010 §4`, `tech-debt` row; `docs/adr/ADR-009-clasificacion-de-tarea.md` decision on Spike/Architectural/Bounded classification; standard §5.3. — fix: reject the skill on bounded scope or duplication grounds if desired; limit the ADR-009 objection to scores that replace or lower mandatory classification. Whether the unavailable upstream skill actually does that remains inconclusive.

- [LOW] **The catalog’s keyword test does not establish platform independence.** — impact: framework-specific material can pass, while ordinary Spanish can fail. — evidence: `PROP-010 §3.2 REQ-P10-01`; an in-memory evaluation of the stated expression accepted `FastAPI` and rejected `reactivar`. The proposed `rg` command also lacks an explicit target path. — fix: treat the expression as an auxiliary search, specify the catalog path and distinguish zero matches from exit status; verify independence through documented review of assumptions and dependencies.

DECISION = fix-and-retry  
PENDING = Correct HIGH findings; correct or explicitly justify MED findings; disposition LOW finding; repeat affected checks and fresh review. Upstream provenance remains inconclusive within this sandbox. Final implementation, budget and Operator acceptance remain pending.  
model = GPT-6, self-declared from environment instructions; effective model identity independently unverified.

## Capa humana (ES)

Operador, la decisión es **fix-and-retry**. Los dos riesgos principales están en las mitigaciones preautorizadas y en la minimización de datos: faltan comprobaciones que impidan ejecutar con una autorización revocada o incorporar identificadores sensibles y borradores al historial.

Los gates actuales pasan y confirman **993/1000 líneas**. La estimación de 995 es aritméticamente correcta, condicionada al texto final. La simulación confirma que `plantillas/v8` puede emitir los avisos de drift previstos sin modificar scripts.

No encontré una prohibición general contra las plantillas de incidentes, siempre que sigan siendo orientación de proceso. El formato compacto es compatible con el archivo histórico y el ADR separado, pero necesita una verificación explícita de las rondas incorporadas: el gate actual no las descubre allí.

También deben corregirse el criterio de salida de «experimental», la interpretación de ADR-009 para rechazar `tech-debt` y la prueba por palabras clave. La procedencia upstream queda inconclusa. No modifiqué archivos ni utilicé la red.
```
